
set path = ( /bin /usr/local/bin /usr/bin )


if ($?prompt) then 
  set prompt = "%m:%~> "
  set rmstar
  set noclobber
endif


if (`filetest -e /etc/complete.tcsh`) then 
  source /etc/complete.tcsh
endif

# ALIASES #
alias la ls -a
alias m more
alias venv source /.venv/bin/activate.csh 

